const storageItem = {
    LOCAL_STORAGE: 'localStorage',
    SESSION_STORAGE: 'sessionStorage',
    INDEXED_DB: 'indexedDB',
    save: save,
    connect: connectDb,
    get: get,
    getAll: getAll,
    saveToIndex: saveToIndex
};

